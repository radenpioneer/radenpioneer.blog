{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dev Server",
      "type": "bun",
      "request": "launch",
      "program": "./node_modules/.bin/astro",
      "args": ["dev", "--host=0.0.0.0"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
