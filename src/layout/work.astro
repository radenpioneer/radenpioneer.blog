---
// Type layout for Portfolio Item routes. Extends Common with elements specific to this content type.
//
// Empty on purpose, and not speculative — see the note in blog.astro. This is the Type tier of
// ticket 15's layer table for Portfolio Item routes, where `CreativeWork` JSON-LD and
// `og:type: website` land. A Portfolio Item describes a standing state rather than an event,
// which is why its Type tier differs from the Blog Post one at all.
import Layout, { type Props as LayoutProps } from "./layout.astro"

export type Props = LayoutProps & {}

const { ...props } = Astro.props
---

<Layout {...props}>
	<slot />

	<Fragment slot="head">
		{/* TODO: Work layout SEO tags */}
		<slot name="head" />
	</Fragment>
</Layout>
